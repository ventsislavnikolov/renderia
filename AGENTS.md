# Renderia — Agent Rules

AI-assisted interior renovation workspace. Domain model, ubiquitous language
(Project, Task, Furniture Library, …), and behaviour contracts live in
`CONTEXT.md` — read it before building anything, and use its vocabulary
verbatim (it lists terms to avoid).
